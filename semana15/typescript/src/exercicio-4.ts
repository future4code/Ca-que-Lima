// a) No terminal usaria o comando tsc
// b) Se nenhuma configuração foi colocada no tsconfig.json, teria que navegar até a pasta src para então dar o comando tsc seguido do nome do arquivo
// c) Se apenas usar o comando tsc ele vai buscar todos os arquivos com extensão .ts